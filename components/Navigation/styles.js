import { makeStyles } from "@material-ui/core/styles";

export default makeStyles(() => ({
  navigation: {
    position: 'fixed',
    bottom: 0,
    left: 0,
    width: '100%',
    zIndex: 1,
    boxShadow: '0px -1px 7px 1px #d4d4d4',
  }
}));